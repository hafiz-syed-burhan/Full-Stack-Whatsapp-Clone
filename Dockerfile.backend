# Backend Dockerfile (assuming Node.js app)
FROM node:16-alpine

# Set working directory
WORKDIR /app

# Install dependencies
COPY backend/package.json ./
RUN npm install

# Copy the rest of the backend files
COPY backend/ ./

# Expose port (assuming backend runs on port 5000)
EXPOSE 5000

# Command to start the backend app
CMD ["npm", "start"]
